![Java](https://img.shields.io/badge/Java-17-blue)
![Spring Boot](https://img.shields.io/badge/Spring%20Boot-3.4.12-green)
![License](https://img.shields.io/badge/License-MIT-yellow)
# Task Tracker - Бэкенд-сервис для трекинга учебных задач

### 📌 О проекте
Бэкенд-сервис для управления учебными задачами с системой аутентификации, ролевой моделью и расширенной статистикой. Проект полностью соответствует техническому заданию.

**Статус проекта:** ✅ Завершен и готов к проверке

## 🚀 Технологический стек
- **Java 17** + **Spring Boot 3.4.12**
- **Spring Security** + **JWT** аутентификация
- **Spring Data JPA** + **PostgreSQL 15**
- **Docker** + **Docker Compose** для базы данных
- **Swagger/OpenAPI 3** документация
- **Maven** сборка

## 📋 Функционал

### 🔐 Аутентификация и авторизация
- Регистрация новых пользователей
- Авторизация с JWT токенами
- Разграничение доступа по ролям (USER/ADMIN)

### 📝 Управление задачами
- Создание, просмотр, обновление, удаление задач
- Изменение статуса задачи (PLANNED, IN_PROGRESS, DONE, CANCELLED)
- Привязка задач к группам

### 🗂️ Управление группами задач
- Создание, переименование, удаление групп задач
- Просмотр задач по группам

### 👨‍💼 Администраторские функции
- Просмотр всех пользователей системы
- Просмотр всех задач в системе
- Статистика по статусам задач (JPQL запросы)
- Статистика по группам задач

## 🏃 Быстрый старт

### Предварительные требования
- Java 17 или выше
- Docker и Docker Compose
- Maven 3.6+

### Шаг 1: Клонирование репозитория
```bash
git clone <ваш-репозиторий>
cd task-tracker
```
### Шаг 2: Запуск базы данных
```bash
docker-compose up -d
```
### Шаг 3: Запуск приложения
```bash
# Вариант 1: Использовать скрипт (рекомендуется)
.\start-app.bat

# Вариант 2: Запуск вручную
.\mvnw.cmd spring-boot:run
```
Приложение будет доступно по адресу: http://localhost:8083
## 📚 API Документация
После запуска доступна интерактивная документация:
- **Swagger UI:** http://localhost:8083/swagger-ui.html
- **OpenAPI спецификация:** http://localhost:8083/v3/api-docs
# Основные эндпоинты
## 🔓 Аутентификация (доступны всем)
- `POST /api/auth/register` - Регистрация нового пользователя
- `POST /api/auth/login` - Вход в систему (получение JWT токена)
## 📝 Задачи (требуют аутентификации)
- `GET /api/tasks` - Получить список своих задач
- `POST /api/tasks` - Создать новую задачу
- `GET /api/tasks/{id}` - Получить задачу по ID
- `PUT /api/tasks/{id}` - Обновить задачу
- `DELETE /api/tasks/{id}` - Удалить задачу
- `PATCH /api/tasks/{id}/status` - Изменить статус задачи

## 🗂️ Группы задач
- `GET /api/groups` - Получить список своих групп

- `POST /api/groups` - Создать новую группу

- `PUT /api/groups/{id}` - Обновить группу

- `DELETE /api/groups/{id}` - Удалить группу

## 👑 Администратор (требуют роль ADMIN)
## 👑 Администратор (требуют роль ADMIN)
- `GET /api/admin/users` - Все пользователи системы
- `GET /api/admin/tasks` - Все задачи системы
- `GET /api/admin/tasks/user/{userId}` - Задачи конкретного пользователя
- `GET /api/admin/statistics/tasks-by-status` - Статистика по статусам
- `GET /api/admin/statistics/tasks-by-group` - Статистика по группам
- `GET /api/admin/statistics/overview` - Общая статистика

## 🗄️ База данных
Docker Compose конфигурация
```yaml
# docker-compose.yml
version: '3.8'
services:
  postgres:
    image: postgres:15-alpine
    container_name: tasktracker-db
    environment:
      POSTGRES_DB: tasktracker
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
    ports:
      - "5432:5432"
```
### Схема базы данных
- **users** - Пользователи (id, email, password, role)

- **tasks** - Задачи (id, title, description, status, user_id, group_id)

- **task_groups** - Группы задач (id, name, user_id)

## 🔐 Безопасность
### Роли пользователей
- **ROLE_USER** - Обычный пользователь (CRUD своих задач)

- **ROLE_ADMIN** - Администратор (полный доступ)

### Тестовые пользователи (создаются автоматически)
- **Администратор:** admin@example.com / admin123

- **Обычный пользователь:** user@example.com / user123

### JWT Аутентификация
1. Получите токен через POST /api/auth/login

2. Добавьте в заголовки запроса:

```text
Authorization: Bearer <ваш_jwt_токен>
```
## 🐛 Поиск и устранение неисправностей
### Приложение не запускается
1. Проверьте, что Java 17 установлена: java -version

2. Проверьте, что PostgreSQL запущен: docker ps

3. Проверьте логи: target/logs/application.log

### Проблемы с аутентификацией
1. Убедитесь, что передаете правильный JWT токен

2. Проверьте, что токен не истек

3. Убедитесь, что пользователь существует

### Проблемы с базой данных
1. Проверьте, что Docker запущен

2. Проверьте контейнер: docker-compose ps

3. Пересоздайте контейнер: docker-compose down && docker-compose up -d

## 📦 Структура проекта
```text
task-tracker/
├── src/
│   ├── main/java/com/tasktracker/
│   │   ├── controller/     # REST контроллеры
│   │   ├── service/        # Бизнес-логика
│   │   ├── repository/     # JPA репозитории
│   │   ├── entity/         # Сущности БД
│   │   ├── dto/           # Data Transfer Objects
│   │   ├── security/      # Spring Security конфигурация
│   │   └── config/        # Конфигурационные классы
│   └── main/resources/
│       ├── application.yml # Конфигурация приложения
│       └── ...
├── docker-compose.yml     # Конфигурация PostgreSQL
├── pom.xml               # Maven зависимости
├── README.md            # Эта документация
└── start-app.bat        # Скрипт запуска для Windows
```
## 📄 Примеры запросов
### Регистрация пользователя
```bash
curl -X POST "http://localhost:8083/api/auth/register" \
  -H "Content-Type: application/json" \
  -d '{"email":"newuser@example.com","password":"password123"}'
```
### Вход в систему
```bash
curl -X POST "http://localhost:8083/api/auth/login" \
  -H "Content-Type: application/json" \
  -d '{"email":"admin@example.com","password":"admin123"}'
```
### Создание задачи (с JWT токеном)
```bash
curl -X POST "http://localhost:8083/api/tasks" \
  -H "Authorization: Bearer ваш_jwt_токен" \
  -H "Content-Type: application/json" \
  -d '{
    "title": "Изучить Spring Boot",
    "description": "Пройти курс по Spring Boot",
    "status": "PLANNED"
  }'
```
## 👥 Тестирование
- При первом запуске автоматически создаются тестовые пользователи

- Все эндпоинты протестированы через Swagger UI

- JPQL запросы для статистики работают корректно

## 📊 Соответствие требованиям ТЗ

✅ Все обязательные сущности реализованы

✅ Регистрация и аутентификация через JWT

✅ Ролевая модель (USER/ADMIN)

✅ JPQL запросы для статистики

✅ Пароли хранятся в хешированном виде

✅ Docker Compose для развертывания БД

✅ Полная документация API